---
description: Core project context for the Refund monorepo
alwaysApply: true
---

# Project core

## Monorepo

- `apps/backend` — Express + Prisma (SQLite) API
- `apps/frontend` — React + Vite SPA
- Do not invent a new monorepo tool; keep the current layout

## Phase 3 scope

- Portfolio foundation — service convention docs, **dev-only Docker Compose**, **API (Supertest) + E2E (Playwright)**
- No Postgres/S3 and no new domain features (approve/reject, etc.)
- Root README stays deferred until post–Phase 3
- Prefer reviewable diffs aligned with [new-features.md](../docs/new-features.md) (fewer, denser PRs)

## Secrets & env

- Never commit real secrets (`.env` with real JWT/password keys)
- Use `.env.example` placeholders only
- JWT secret and API URLs come from environment, not hardcoded literals

## Language

- Chat and explanations with the user: Portuguese (pt-BR)
- Commit messages, PR titles/bodies, code identifiers, and code comments: English
---
