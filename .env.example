# Copy to `.env` at the repo root to override Compose defaults (optional).
# App-specific env for local non-Docker runs lives in apps/*/.env.example.

BACKEND_PORT=3333
FRONTEND_PORT=5173
JWT_SECRET=dev-jwt-secret-change-me
VITE_API_URL=http://localhost:3333
